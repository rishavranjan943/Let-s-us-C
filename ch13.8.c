#include<stdio.h>
int main()
{
    int max;
    scanf("%d",&max);
    int arr[max];
    for(int i=0;i<max;i++)
    {
        printf("Enter the value : ");
        scanf("%d",&arr[max]);
    }
    return 0;
}